# app/Dockerfile builds with this repo root as its context (see the
# Dockerfile's own header comment and README "Deploying") — this file has
# to live here, not in app/, for Docker to actually pick it up.
node_modules
app/node_modules
build
app/build
.cache
app/.cache
.env
.env.*
app/.env
app/.env.*
.git
.shopify
app/.shopify
.react-router
app/.react-router
dist
