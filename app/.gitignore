node_modules

# macOS
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build

.env
.env.*
!.env.example

# package-lock.json is intentionally tracked (not ignored) — npm ci in
# Dockerfile/CI needs it, and a fresh git clone (which is exactly what
# Railway/Render/Fly build from) won't have one otherwise.
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Hide files auto-generated by react router
.react-router/

